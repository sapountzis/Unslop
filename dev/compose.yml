version: "3.9"
services:
  postgres:
    image: postgres:18.1
    environment:
      POSTGRES_USER: unslop
      POSTGRES_PASSWORD: unslop_dev_password
      POSTGRES_DB: unslop
    ports:
      - "5432:5432"
    volumes:
      - unslop-postgres:/var/lib/postgresql/data
volumes:
  unslop-postgres:
